<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >
    <!-- 设置背景图片 -->
    <item android:id="@android:id/background" android:drawable="@drawable/seekbar_bg"></item>
    
	<item android:id="@android:id/secondaryProgress" android:drawable="@drawable/seekbar_second">
	</item>
	<!-- 设置进度条图片，需要添加clip，否则会把整个进度条填满 -->
	<item android:id="@android:id/progress" android:drawable="@drawable/seekbar_progress">	</item>
</layer-list>
